\providecommand{\devnum}[1]{#1}

\section{Experiments}
\label{sec:experiments}
\label{sec:exp_setup}

Every policy is simulated on the shared pool of Section~\ref{sec:overlay}, with the same
arrivals and costs, so each comparison is made job by job. Supplementary Table~S4 lists the
configuration.

\subsection{The Headline: Scores That Use Only Available Results}
\label{sec:exp_visibility}

The prediction study of Section~\ref{sec:prediction} releases a job's result at the time the
original platform recorded it. In the simulated queue, a job completes at a time that
depends on the policy, so a score computed on the original clock may use a result that the
simulated queue has not produced yet. The \emph{online} protocol removes this for each copy
of a class-term. It freezes the fitted model and simulates each policy once. When a
job arrives, the model scores it from the results of earlier jobs in the same copy of its
class-term that this simulation has completed by that moment, and from the recorded results
of other classes and terms, which keep their original times. A score therefore follows the
simulated queue for its job's own copy and the recorded clock for all others. Supplementary Section~S3.7 defines the
simulation and checks it against a second implementation.

Four references stand beside the online protocol. The \emph{exact} protocol also freezes
the model. It simulates a policy, withholds every result that was not yet complete when the
job using it arrived, rescores, and simulates again until no score uses an unavailable
result. A result it has withheld once stays withheld, even when a later simulation
completes it in time, so it can use less history than the online protocol. The
\emph{original} protocol keeps the platform's clock and is optimistic. The
\emph{conservative} protocol admits a result only an hour after it appeared, uses only
history from the same class-term copy, and refits the models. The \emph{static} protocol
uses no outcome history at all. The guard's parameters are the same under all five.

At the busiest load, Guard($\devnum{600}$) under the online protocol reaches a
$\mathrm{p99}_{\mathrm{dl}}$ of \devnum{111.25} s and closes \devnum{0.705 [0.648, 0.768]} of
the gap (Table~\ref{tab:visibility}). The same guard closes \devnum{0.672 [0.602, 0.743]}
under the exact protocol, \devnum{0.801 [0.764, 0.842]} under the original one,
\devnum{0.429 [0.357, 0.533]} under the conservative one and \devnum{0.349 [0.281, 0.457]}
under the static one. The online figure lies between the exact and the original ones. The
exact protocol uses less history than the online one, and the original protocol uses
results the queue has not produced. At the two lighter loads the two figures are closer,
\devnum{0.725} against \devnum{0.740} and \devnum{0.800} against \devnum{0.837}, and their
intervals overlap. Unguarded SPJF-E closes \devnum{0.864} under the online protocol. The
conservative figure falls further than the exact one because it also withholds history that
the queue would have had and refits the models. Supplementary Section~S3.7 separates the
two effects.
The parameters were selected under the original protocol.
% PENDING RESELECTION: the restricted online reselection result goes here.
The full grids were run under the original protocol, so the rest of this section reports it
unless stated otherwise, and its figures are optimistic references. Supplementary
Section~S11 replays \devnum{24} of the compared policies under the online protocol: the two
unguarded rankings, the aging baseline, and at each promise the guard, its three
single-shape winners, Fixed, Skip and Timeout. There every one of them closes less of the
gap at the two higher loads and none with a promise exceeds it. Every paired difference that
clears zero on the original clock keeps its sign, four no longer clear zero and three newly
do, and the one conclusion of this section that changes is the choice between the two
rankings in Section~\ref{sec:exp_rank}.
% Sources of the S11 pointer: outputs/dev_online_visibility/online_comparison.csv (24 base
% policies, gap_closed online below original for all of them at levels 1 and 2;
% max_excess_s below the promise in every row) and outputs/online_paired/
% online_paired_differences.csv (scripts/online_paired_differences.py).
% Sources: outputs/dev_online_visibility/online_comparison.csv, level=2, Guard(600)|online
% and SPJF-E|online: p99_dl_s, gap_closed, gap_closed_lo/hi.
% outputs/dev_consistent_visibility/exact_comparison.csv, level=2, Guard(600)|exact,
% Guard(600)|original, Guard(600)|conservative, Guard(600)|static. Levels 0 and 1:
% Guard(600)|online 0.725, 0.800 and Guard(600)|original 0.740, 0.837.

\begin{table}[htbp]
\centering
\small
\caption{Guard(600) under five protocols for what the scores may use. Online: each score
uses the results of its own class-term copy completed by its job's arrival in the simulated
queue, and the recorded results of other classes and terms (the headline).
Exact: results withheld and the queue simulated again until no score uses an unavailable
result; a withheld result stays withheld. Original: the platform's own clock (optimistic).
Conservative and static withhold more than the queue requires. Gap closed with
\devnum{95\%} intervals from resampled weeks (Section~\ref{sec:overlay}), which do not cover
the choice of classes and terms; waits, largest excess and harm in seconds. Fired: share of
dispatch decisions, weighted by queue length, at which the guard overrode the base policy.
At $\rho = \devnum{0.5}$, $k = \devnum{7.8}$ is the mean over the five overlays.}
\label{tab:visibility}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{llrlrrr}
\toprule
Load & Protocol & p99$_{\mathrm{dl}}$ & Gap closed & Max excess & Harm & Fired (\%) \\
\midrule
\multicolumn{7}{l}{$\rho = \devnum{0.5}$, $k = \devnum{7.8}$} \\
 & online         & \devnum{15.22} & \devnum{0.725 [0.696, 0.746]} & \devnum{499.5} & \devnum{248.3} & \devnum{0.38} \\
 & exact          & \devnum{15.25} & \devnum{0.724 [0.697, 0.747]} & \devnum{498.6} & \devnum{248.1} & \devnum{0.45} \\
 & original       & \devnum{14.97} & \devnum{0.740 [0.713, 0.760]} & \devnum{492.2} & \devnum{237.0} & \devnum{0.17} \\
 & conservative   & \devnum{16.68} & \devnum{0.640 [0.604, 0.671]} & \devnum{499.9} & \devnum{235.2} & \devnum{0.94} \\
 & static         & \devnum{17.95} & \devnum{0.560 [0.519, 0.594]} & \devnum{498.0} & \devnum{225.1} & \devnum{0.38} \\
\midrule
\multicolumn{7}{l}{$\rho = \devnum{0.8}$, $k = \devnum{5}$} \\
 & online         & \devnum{50.57} & \devnum{0.800 [0.772, 0.819]} & \devnum{509.3} & \devnum{215.7} & \devnum{2.18} \\
 & exact          & \devnum{51.90} & \devnum{0.785 [0.748, 0.807]} & \devnum{509.0} & \devnum{215.6} & \devnum{2.64} \\
 & original       & \devnum{47.47} & \devnum{0.837 [0.815, 0.851]} & \devnum{507.8} & \devnum{319.6} & \devnum{1.31} \\
 & conservative   & \devnum{64.77} & \devnum{0.634 [0.560, 0.688]} & \devnum{509.4} & \devnum{327.7} & \devnum{3.26} \\
 & static         & \devnum{70.18} & \devnum{0.571 [0.504, 0.638]} & \devnum{512.9} & \devnum{341.6} & \devnum{3.89} \\
\midrule
\multicolumn{7}{l}{$\rho = \devnum{1}$, $k = \devnum{4}$} \\
 & online         & \devnum{111.25} & \devnum{0.705 [0.648, 0.768]} & \devnum{517.7} & \devnum{333.2} & \devnum{7.27} \\
 & exact          & \devnum{118.94} & \devnum{0.672 [0.602, 0.743]} & \devnum{519.9} & \devnum{332.8} & \devnum{7.82} \\
 & original       & \devnum{89.24} & \devnum{0.801 [0.764, 0.842]} & \devnum{523.7} & \devnum{339.9} & \devnum{7.15} \\
 & conservative   & \devnum{174.71} & \devnum{0.429 [0.357, 0.533]} & \devnum{527.8} & \devnum{303.5} & \devnum{7.71} \\
 & static         & \devnum{193.11} & \devnum{0.349 [0.281, 0.457]} & \devnum{528.1} & \devnum{316.1} & \devnum{11.18} \\
\bottomrule
\end{tabular}
\end{table}
% Source of Table~\ref{tab:visibility}: outputs/consistent_paper_tables/tab_visibility.tex,
%   produced by `python scripts/emit_paper_tables.py --out-dir outputs/consistent_paper_tables
%   --dev-exact-dir outputs/dev_consistent_visibility --dev-online-dir
%   outputs/dev_online_visibility` (see GENERATED.md), from
%   outputs/dev_online_visibility/online_comparison.csv (the online rows) and
%   outputs/dev_consistent_visibility/exact_comparison.csv (the other four), base_policy
%   Guard(600), all three levels; columns p99_dl_s, gap_closed with gap_closed_lo/hi,
%   max_excess_s, harm_s, fired_pct.

\subsection{The Sealed Terms}
\label{sec:exp_sealed}

The three sealed terms of the main log were opened once, on 25 September 2026, after the
method, its parameters and the primary metric had been frozen (Section~\ref{sec:sealed}).
The run replayed the frozen configuration and code and selected nothing, and the access
ledger in the repository lists each of its ten commands and what each produced. The sealed
terms hold fewer class-terms than the development pool, so each of their five overlays
superposes 31 copies over 29 weeks. The number of servers follows the rule of
Section~\ref{sec:load}, and the busiest hours reach utilisations of 0.487, 0.770 and 0.954
at the three loads.
% Sources: docs/sealed_access_log.md, rows of 2026-09-25 under lock f6c1b3af
% (build_overlays.py: 5 overlays, 31 copies each, 29 whole weeks);
% outputs/sealed_tables/main_table.csv, FCFS rows, rho_realised 0.487, 0.770, 0.954.

At the busiest load, Guard(600) under the online protocol reaches a
$\mathrm{p99}_{\mathrm{dl}}$ of \sealednum{47.22} s and closes \sealednum{0.894}
[\sealednum{0.862}, \sealednum{0.912}] of the gap (Table~\ref{tab:visibility_sealed}). It closes
\sealednum{0.880} under the exact protocol and \sealednum{0.911} under the original one, and
the five protocols fall in the same order as on the development terms. Unguarded SPJF-E
closes \sealednum{0.936} [\sealednum{0.909}, \sealednum{0.948}] online. The guard's largest excess over FCFS is
\sealednum{533.8} s against its promise of 600 s, and its harm is \sealednum{237.5} s,
where SPJF-E reaches \sealednum{3{,}301.6} s and \sealednum{1{,}602.0} s. No guard in the
main comparison exceeds its promise at any load (Supplementary Section~S10).

Every policy closes a larger share of the gap on the sealed terms than on the development
ones, except five of the deliberately corrupted rankings, which do more damage there. The
sealed run therefore confirms the guarantee and the order of the policies, but not the
size of the development figures. The running-time predictor ranks the sealed
terms as well as the development ones, with a pooled AUROC of \sealednum{0.9414} for
SPJF-E's score against \devnum{0.9360} on the development terms.
% Sources: outputs/sealed_online_visibility/online_comparison.csv, level=2,
% Guard(600)|online and SPJF-E|online (p99_dl_s, gap_closed and lo/hi, max_excess_s,
% harm_s); outputs/sealed_consistent_visibility/exact_comparison.csv, level=2,
% Guard(600)|exact and Guard(600)|original; outputs/sealed_tables/main_table.csv against
% outputs/dev_tables/main_table.csv (gap_closed higher on 76 of 81 policy-levels; the five
% lower are SPJF-reversed, Guard(600)-reversed and Guard(600)-top1short; max_excess_s at
% most the promise for every Guard and Fixed row); outputs/sealed_predictor and
% outputs/dev_predictor predictor_metrics.csv, pooled spjf_e auroc.

At the busiest load the guard closes \sealednum{0.189} more of the gap on the sealed terms
under the online protocol. Unguarded SPJF-E closes \sealednum{0.072} more, and the guard's
cost against SPJF-E falls from \devnum{0.158} to \sealednum{0.041}, which gives the other
\sealednum{0.117}. The load on the two pools does not account for the difference. One cell from each pool has
four servers and a busiest-hour utilisation of \devnum{0.980} and \sealednum{0.979}, and
FCFS and SJF differ by \devnum{220.1} s and \sealednum{223.6} s in
$\mathrm{p99}_{\mathrm{dl}}$ in these two cells; the guard closes \devnum{0.681} of the gap
in the first and \sealednum{0.889} in the second. The larger share on the sealed terms is confined to
the primary metric. The jobs that this metric reads, those arriving in the 24 hours before
a deadline, are \sealednum{37.5}\% of the sealed jobs against \devnum{23.9}\% of the
development ones. On the
99th-percentile wait over all jobs, the same ratio gives the guard \devnum{0.904} and
\sealednum{0.896} and SPJF-E \devnum{0.956} and \sealednum{0.932}, and on the mean wait
SPJF-E closes less on the sealed terms at all three loads (\sealednum{0.819} against
\devnum{0.903} at the busiest).
% Source: outputs/sealed_dev_contrast/contrast.csv (scripts/sealed_dev_contrast.py):
% difference, ranking_part, cost_part and guard_cost at level 2; matched_* rows (dev
% overlay 2, sealed overlay 0); deadline_share_pct; gap_all and gap_mean.

\begin{table}[htbp]
\centering
\small
\caption{Guard(600) under the five protocols of Table~\ref{tab:visibility} on the three
sealed terms, opened once after the method and its parameters were frozen. Columns as in
Table~\ref{tab:visibility}; $k$ is the mean over the five overlays.}
\label{tab:visibility_sealed}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{llrlrrr}
\toprule
Load & Protocol & p99$_{\mathrm{dl}}$ & Gap closed & Max exc. & Harm & Fired (\%) \\
\midrule
\multicolumn{7}{l}{$\rho = \sealednum{0.5}$, $k = \sealednum{8.2}$} \\
 & online         & \sealednum{3.76} & \sealednum{0.895 [0.872, 0.906]} & \sealednum{260.8} & \sealednum{166.2} & \sealednum{2.59} \\
 & exact          & \sealednum{3.77} & \sealednum{0.893 [0.871, 0.903]} & \sealednum{256.6} & \sealednum{166.6} & \sealednum{3.29} \\
 & original       & \sealednum{3.75} & \sealednum{0.897 [0.878, 0.907]} & \sealednum{264.0} & \sealednum{173.9} & \sealednum{4.01} \\
 & conservative   & \sealednum{4.15} & \sealednum{0.839 [0.816, 0.858]} & \sealednum{264.1} & \sealednum{178.0} & \sealednum{3.40} \\
 & static         & \sealednum{4.46} & \sealednum{0.792 [0.744, 0.824]} & \sealednum{259.3} & \sealednum{203.3} & \sealednum{0.16} \\
\midrule
\multicolumn{7}{l}{$\rho = \sealednum{0.8}$, $k = \sealednum{5.2}$} \\
 & online         & \sealednum{24.59} & \sealednum{0.884 [0.862, 0.903]} & \sealednum{506.6} & \sealednum{202.6} & \sealednum{6.40} \\
 & exact          & \sealednum{24.74} & \sealednum{0.880 [0.860, 0.899]} & \sealednum{499.2} & \sealednum{202.6} & \sealednum{4.41} \\
 & original       & \sealednum{24.22} & \sealednum{0.891 [0.874, 0.908]} & \sealednum{504.6} & \sealednum{189.5} & \sealednum{7.01} \\
 & conservative   & \sealednum{28.09} & \sealednum{0.811 [0.772, 0.845]} & \sealednum{520.3} & \sealednum{233.2} & \sealednum{7.06} \\
 & static         & \sealednum{31.70} & \sealednum{0.740 [0.691, 0.786]} & \sealednum{533.7} & \sealednum{255.3} & \sealednum{1.33} \\
\midrule
\multicolumn{7}{l}{$\rho = \sealednum{1}$, $k = \sealednum{4.2}$} \\
 & online         & \sealednum{47.22} & \sealednum{0.894 [0.862, 0.912]} & \sealednum{533.8} & \sealednum{237.5} & \sealednum{7.65} \\
 & exact          & \sealednum{49.40} & \sealednum{0.880 [0.839, 0.903]} & \sealednum{526.5} & \sealednum{243.1} & \sealednum{5.25} \\
 & original       & \sealednum{44.33} & \sealednum{0.911 [0.890, 0.926]} & \sealednum{522.9} & \sealednum{293.2} & \sealednum{7.10} \\
 & conservative   & \sealednum{65.08} & \sealednum{0.784 [0.712, 0.845]} & \sealednum{518.8} & \sealednum{244.6} & \sealednum{8.57} \\
 & static         & \sealednum{82.66} & \sealednum{0.679 [0.583, 0.783]} & \sealednum{528.6} & \sealednum{332.3} & \sealednum{3.20} \\
\bottomrule
\end{tabular}
\end{table}
% Source of Table~\ref{tab:visibility_sealed}:
%   outputs/consistent_paper_tables/tab_visibility_sealed.tex, emitted with the --sealed-*
%   options of scripts/emit_paper_tables.py (docs/sealed_run_procedure.md, Section 3).

\subsection{What to Rank By}
\label{sec:exp_rank}

On the original clock, ranking by expected cost beats the log-scale score at the two higher
loads and ties with it at the lightest. The paired difference in gap closed is
\devnum{$-0.000$ $[-0.016, 0.013]$}, \devnum{$+0.012$ $[0.004, 0.019]$} and
\devnum{$+0.019$ $[0.011, 0.024]$} at $\rho = \devnum{0.5}$, \devnum{0.8} and \devnum{1.0}.
Refitting the log-scale model from scratch moves this difference by at most \devnum{0.001}
(Supplementary Table~S16). The online protocol does not confirm this order. There the three
differences are \devnum{$-0.022$ $[-0.039, -0.009]$}, \devnum{$+0.006$ $[-0.004, 0.015]$}
and \devnum{$+0.009$ $[-0.000, 0.015]$}, so the log-scale score is ahead at the lightest load
and the two tie at the higher ones (Supplementary Table~S32). Both scores are replayed with
their own frozen models, so the change comes from the history each may use. The choice of
SPJF-E as the base ranking therefore rests on the original protocol. On the original clock,
quantile scores and squared error on raw seconds lose to the Tweedie fit at every load. A
rule that only
separates predicted-heavy from predicted-light jobs recovers little of the benefit, so most
of the benefit comes from the order inside the light class (Supplementary Table~S14).
% Source of the online differences: outputs/online_paired/online_paired_differences.csv,
% variant online, left SPJF-E, right SPJF-log, levels 0-2 (difference_gap, gap_lo, gap_hi),
% written by scripts/online_paired_differences.py.

The two kinds of prediction error do not cost the same. Replacing the largest
\devnum{1\%} of underestimates by the true cost closes \devnum{0.91} of the distance to SJF
in $\mathrm{p99}_{\mathrm{dl}}$ at $\rho = \devnum{1.0}$ on both overlays tested. Repairing
the largest \devnum{1\%} of overestimates closes only \devnum{0.26} and \devnum{0.24}. At
\devnum{0.1\%}, the repair of underestimates closes \devnum{0.47} and that of overestimates
\devnum{0.04}. Treating a long job as short is thus the expensive mistake, by about an
order of magnitude. The repair depends on the score's numerical scale, so this result
describes our fitted predictor and not prediction errors in general.
% Source of the asymmetry figures: evidence/ranking_score/out_sim_primary_rep0_mech.txt and
%   out_sim_primary_rep1_mech.txt, blocks primary_rep{0,1}_r1_mech, column w_p99_dl.

\subsection{What the Guarantee Costs and Gains}
\label{sec:exp_guard}

Table~\ref{tab:guard} gives the guard at three bounds and three loads. At $\rho =
\devnum{1.0}$, Guard($\devnum{600}$) closes \devnum{0.801} of the gap and the unguarded
policy \devnum{0.916}. A bound of ten time limits therefore costs \devnum{0.115} of the gap.
The cost is \devnum{0.072} at $G = \devnum{1200}$ s and \devnum{0.438} at $G =
\devnum{300}$ s. The paired intervals of these three differences are
\devnum{[-0.142, -0.077]}, \devnum{[-0.083, -0.052]} and \devnum{[-0.526, -0.314]}, and none
contains zero, so the guarantee has a cost at every $G$ we tested. The cost falls as the
slack above the floor of Corollary~\ref{cor:params}, $G - (3-2/k)L$, grows against FCFS's
$\mathrm{p99}_{\mathrm{dl}}$ of \devnum{273.50} s: the slack is \devnum{0.55},
\devnum{1.65} and \devnum{3.84} times that wait at $G = \devnum{300}$, \devnum{600} and
\devnum{1200} s. Under the online protocol
the cost is larger, \devnum{0.158 [0.110, 0.191]}, \devnum{0.110 [0.082, 0.124]} and
\devnum{0.497 [0.393, 0.565]} (Supplementary Table~S32). What the guarantee buys
is protection of the worst job. The unguarded policy delays one job at the busiest load by
\devnum{5{,}904.5} s beyond its FCFS wait, \devnum{98.4} times the \devnum{60}-second limit.
Exact SJF, which knows every running time, delays one job by \devnum{4{,}755.5} s,
\devnum{79.3} limits, so most of this delay comes from ordering by length and not from
wrong predictions.
The guard keeps every job within its bound. It also keeps the worst wait among heavy jobs
within about \devnum{450} s of its FCFS value, whereas the unguarded policy multiplies that
wait by \devnum{2.8} to \devnum{4.1}. The price of this protection is paid in the mean and
just below the worst job. At the busiest load the guard keeps \devnum{0.751} of SJF's reduction of the mean
wait, against \devnum{0.930} unguarded, and its 99.9th-percentile wait over all jobs is
\devnum{717.6} s, against \devnum{192.4} s unguarded and \devnum{984.3} s under FCFS.
% Mean: outputs/dev_tables/main_table.csv, level 2, mean_s, (FCFS - policy)/(FCFS - SJF)
% = (10.270 - 3.830)/(10.270 - 1.696) and (10.270 - 2.294)/(10.270 - 1.696).
% p99.9: outputs/dev_visibility/visibility_waits_and_lag.csv, level 2, wait_p999_s mean
% over the five overlays: Guard(600)-original 717.57, SPJF-E-original 192.44, FCFS 984.31.
% Source: outputs/dev_tables/paired_differences.csv, rows Guard(G) - SPJF-E, rho = 1.0;
% online: outputs/online_paired/online_paired_differences.csv, variant online, level 2,
% Guard(G) - SPJF-E, printed as costs (sign reversed, interval ends swapped);
% the two gaps 0.801 and 0.916 are main_table.csv rows 71 and 62, gap_closed.
% 98.4 = SPJF-E rho 1.0 max_excess_s 5904.525 / 60 (row 62); 79.3 = SJF rho 1.0 max_excess_s
% 4755.467 / 60. 450 = largest Guard(600) - FCFS
% max_heavy_s over the three loads (433.95, 444.01, 450.14). 2.8 and 4.1 = SPJF-E / FCFS
% max_heavy_s at rho 0.5 (1374.79 / 486.87) and rho 1.0 (6286.25 / 1542.75).

% Wider than the MDPI text block: set across the full page with the template's
% adjustwidth/\extralength mechanism.
\begin{table}[htbp]
\begin{adjustwidth}{-\extralength}{0cm}
\centering
\caption{Guard($G$) at three bounds and three loads, original protocol, with the FCFS
and SPJF-E figures of each load in its header. Gap closed with \devnum{95\%} intervals.
Reduction is the plain percentage reduction of $\mathrm{p99}_{\mathrm{dl}}$ against FCFS,
which is a different number. Largest excess and harm are the worst over the five overlays,
and the other columns are means. Times in seconds. Fired as in Table~\ref{tab:visibility}.
Supplementary Table~S6 adds the comparators.}
\label{tab:guard}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{llrrlrrrr}
\toprule
Load & Policy & $G$ (s) & p99$_{\mathrm{dl}}$ & Gap closed & Reduction (\%) & Max excess & Harm & Fired (\%) \\
\midrule
\multicolumn{9}{l}{$\rho = \devnum{0.5}$, $k = \devnum{7.8}$; FCFS p99$_{\mathrm{dl}} = \devnum{27.64}$ s, SPJF-E \devnum{14.68} s with max excess \devnum{1{,}241.4} s} \\
 & Guard($\devnum{300}$)      & \devnum{300} & \devnum{15.58} & \devnum{0.705 [0.678, 0.724]} & \devnum{43.7} & \devnum{192.5} & \devnum{132.1} & \devnum{1.35} \\
 & Guard($\devnum{600}$)      & \devnum{600} & \devnum{14.97} & \devnum{0.740 [0.713, 0.760]} & \devnum{45.8} & \devnum{492.2} & \devnum{237.0} & \devnum{0.17} \\
 & Guard($\devnum{1200}$)     & \devnum{1200} & \devnum{14.78} & \devnum{0.750 [0.723, 0.772]} & \devnum{46.4} & \devnum{996.7} & \devnum{106.0} & \devnum{0.09} \\
\midrule
\multicolumn{9}{l}{$\rho = \devnum{0.8}$, $k = \devnum{5}$; FCFS p99$_{\mathrm{dl}} = \devnum{119.35}$ s, SPJF-E \devnum{43.33} s with max excess \devnum{3{,}044.5} s} \\
 & Guard($\devnum{300}$)      & \devnum{300} & \devnum{55.94} & \devnum{0.738 [0.697, 0.761]} & \devnum{53.0} & \devnum{209.7} & \devnum{114.1} & \devnum{11.57} \\
 & Guard($\devnum{600}$)      & \devnum{600} & \devnum{47.47} & \devnum{0.837 [0.815, 0.851]} & \devnum{60.1} & \devnum{507.8} & \devnum{319.6} & \devnum{1.31} \\
 & Guard($\devnum{1200}$)     & \devnum{1200} & \devnum{46.31} & \devnum{0.850 [0.829, 0.865]} & \devnum{61.1} & \devnum{1{,}096.1} & \devnum{117.7} & \devnum{0.41} \\
\midrule
\multicolumn{9}{l}{$\rho = \devnum{1.0}$, $k = \devnum{4}$; FCFS p99$_{\mathrm{dl}} = \devnum{273.50}$ s, SPJF-E \devnum{62.79} s with max excess \devnum{5{,}904.5} s} \\
 & Guard($\devnum{300}$)      & \devnum{300} & \devnum{163.40} & \devnum{0.478 [0.387, 0.602]} & \devnum{40.2} & \devnum{229.1} & \devnum{134.8} & \devnum{28.64} \\
 & Guard($\devnum{600}$)      & \devnum{600} & \devnum{89.24} & \devnum{0.801 [0.764, 0.842]} & \devnum{67.3} & \devnum{523.7} & \devnum{339.9} & \devnum{7.15} \\
 & Guard($\devnum{1200}$)     & \devnum{1200} & \devnum{79.32} & \devnum{0.844 [0.817, 0.870]} & \devnum{70.9} & \devnum{1{,}110.4} & \devnum{123.4} & \devnum{1.40} \\
\bottomrule
\end{tabular}
\end{adjustwidth}
\end{table}
% Source of Table~\ref{tab:guard}: the nine Guard(G) rows and the three block headers of
%   the full table, which is Supplementary Table S6 (label tab:s_guard) and carries the
%   source comment; check_paper_numbers.py registers this copy as a subset of tab:s_guard.

The harm limit used in selection does not always carry over to the evaluation overlays.
Guard($\devnum{600}$) has harms of \devnum{319.6} and \devnum{339.9} s at the two higher
loads, above the \devnum{300} s validation limit, although every excess stays within the
bound. The shape of the budget matters mainly where the bound is tight and the load high.
There it decides which jobs are delayed more than how much of the benefit survives
(Supplementary Sections~S3.3 and~S3.11). Counting overtakes instead of work gains almost
nothing on this load (Supplementary Figure~S2). Overtakers here are mostly shorter than a
second, while a count must be sized against the worst case of $L$ per pass. Our count
baseline is charged when an overtaker starts. Charging it at completion instead changes its
gap closed by at most \devnum{0.015}, and by at most \devnum{0.001} at the two higher loads.
An aging baseline ranks by $\hat C_i - \omega(t - a_i)$, with the credit $\omega$ chosen
under the same harm limit. At the busiest load under the conservative protocol it closes
\devnum{0.051} of the gap, against \devnum{0.429} for the guard, and one job exceeds its
FCFS wait by \devnum{855.6} s. Under the online protocol it closes \devnum{0.258} against
\devnum{0.705}, and one job exceeds its FCFS wait by \devnum{1{,}808.9} s. Aging carries no
proved bound (Supplementary Tables~S12 and~S31).
% Sources: outputs/dev_tables/main_table.csv, Guard(600), levels=1,2, harm_s;
% evidence/main_v3/v31/report_diff.csv (Skip charged at completion, as in Supplementary
% Section S3.16); outputs/dev_visibility/visibility_comparison.csv, level=2, Aging(600);
% online: outputs/dev_online_visibility/online_comparison.csv, level=2, Aging(600)|online
% and Guard(600)|online, gap_closed and max_excess_s.

A maximum waiting time gives a guarantee of the same form (Proposition~\ref{prop:clock}), so
the guard's counter has to earn its place against it. Table~\ref{tab:clock} runs
Timeout($G - (3-2/k)L$) beside Guard($G$), which gives both the bound $G$. At $\rho =
\devnum{0.5}$ the two differ by at most \devnum{0.053} of the gap. Near saturation they
part. At $\rho = \devnum{1.0}$ and $G = \devnum{600}$ s the clock closes \devnum{0.420} and
the guard \devnum{0.801}. At $G = \devnum{300}$ s the clock closes \devnum{0.031}, and its
$\mathrm{p99}_{\mathrm{dl}}$ of \devnum{266.4} s is barely below FCFS's \devnum{273.5} s. At
$G = \devnum{1200}$ s the two agree again. The clock's bound is not known to be tight, so
we also compare the two at equal realised worst excess. On the first overlay at the busiest
load, Timeout with $\theta = \devnum{480}$ s reaches a largest excess of \devnum{506.1} s and
closes \devnum{0.490} of the gap. Guard($\devnum{600}$) reaches \devnum{512.3} s and closes
\devnum{0.797}. The clock also harms more, \devnum{456.7} s against \devnum{339.9} s at
$\rho = \devnum{1.0}$ and $G = \devnum{600}$ s. Under the online protocol every difference of
Table~\ref{tab:clock} keeps its sign (Supplementary Table~S33). At $\rho = \devnum{1.0}$ and
$G = \devnum{600}$ s the clock closes \devnum{0.282} and the guard \devnum{0.705}, a paired
difference of \devnum{$+0.423$ [$+0.204$, $+0.664$]}, and at $G = \devnum{300}$ s the clock
closes \devnum{0.023}.
% Sources: prechecks/timeout_rule/timeout_intervals.csv and out_summarise.txt (Table
% tab:clock and the harms); evidence/timeout_rule/out_timeout_overlays.txt, overlay 0 level 2,
% Timeout-grid theta 480 (max_excess 506.079, gap 0.4897) and Guard(600) (512.304, 0.7972).
% Online: outputs/online_paired/online_clock_intervals.csv, variant online, level 2 (promise
% 600: guard_gap, timeout_gap, gap_difference with lo/hi; promise 300: timeout_gap); the
% nine online differences have the signs of the nine original ones.

\begin{table}[htbp]
\begin{adjustwidth}{-\extralength}{0cm}
\centering
\caption{Guard($G$) against the maximum waiting time Timeout($G - (3-2/k)L$), which has the
same bound $G$. Gap closed, mean over the five overlays, with \devnum{95\%} intervals. The
difference is Guard minus Timeout, resampled as a difference. Harm is the worst over the
overlays, in seconds. Original protocol.}
\label{tab:clock}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{llllll}
\toprule
Load & $G$ (s) & Guard($G$) & Timeout & Difference & Harm, Guard / Timeout \\
\midrule
\devnum{0.5} & \devnum{300} & \devnum{0.705 [0.678, 0.724]} & \devnum{0.652 [0.619, 0.679]} & \devnum{$+0.053$ [$+0.030$, $+0.077$]} & \devnum{132.1} / \devnum{148.0} \\
 & \devnum{600} & \devnum{0.740 [0.713, 0.760]} & \devnum{0.734 [0.707, 0.753]} & \devnum{$+0.006$ [$+0.001$, $+0.012$]} & \devnum{237.0} / \devnum{237.0} \\
 & \devnum{1200} & \devnum{0.750 [0.723, 0.772]} & \devnum{0.752 [0.724, 0.774]} & \devnum{$-0.002$ [$-0.005$, $+0.001$]} & \devnum{106.0} / \devnum{237.0} \\
\midrule
\devnum{0.8} & \devnum{300} & \devnum{0.738 [0.697, 0.761]} & \devnum{0.244 [0.017, 0.508]} & \devnum{$+0.494$ [$+0.241$, $+0.691$]} & \devnum{114.1} / \devnum{177.1} \\
 & \devnum{600} & \devnum{0.837 [0.815, 0.851]} & \devnum{0.789 [0.754, 0.807]} & \devnum{$+0.048$ [$+0.030$, $+0.080$]} & \devnum{319.6} / \devnum{443.7} \\
 & \devnum{1200} & \devnum{0.850 [0.829, 0.865]} & \devnum{0.857 [0.837, 0.869]} & \devnum{$-0.006$ [$-0.012$, $-0.001$]} & \devnum{117.7} / \devnum{469.2} \\
\midrule
\devnum{1.0} & \devnum{300} & \devnum{0.478 [0.387, 0.602]} & \devnum{0.031 [0.012, 0.055]} & \devnum{$+0.447$ [$+0.369$, $+0.550$]} & \devnum{134.8} / \devnum{182.3} \\
 & \devnum{600} & \devnum{0.801 [0.764, 0.842]} & \devnum{0.420 [0.131, 0.673]} & \devnum{$+0.381$ [$+0.158$, $+0.650$]} & \devnum{339.9} / \devnum{456.7} \\
 & \devnum{1200} & \devnum{0.844 [0.817, 0.870]} & \devnum{0.827 [0.786, 0.865]} & \devnum{$+0.017$ [$-0.002$, $+0.044$]} & \devnum{123.4} / \devnum{708.8} \\
\bottomrule
\end{tabular}
\end{adjustwidth}
\end{table}
% Sources: prechecks/timeout_rule/timeout_intervals.csv (gap closed and paired differences
%   at six decimals, 2,000 week-block resamples, seed 20260919) and out_summarise.txt (worst
%   harm over the five overlays; the p99 266.41 s of Timeout(300) and 273.50 s of FCFS at
%   rho = 1.0). Separate kernel checked job for job against the package's FCFS and SPJF-E.

Ordering by predicted cost inside fixed windows, as in the closest scheduler for judging
queues \cite{zhang2024ojscheduling}, limits overtaking by position. We ran a version of it
on the first overlay with windows of up to \devnum{512} jobs. It closes at most
\devnum{0.417}, \devnum{0.146} and \devnum{0.057} of the gap at the three loads, with
largest excesses of \devnum{232.7}, \devnum{359.6} and \devnum{444.5} s. On the same overlay
Guard($\devnum{300}$) closes \devnum{0.678}, \devnum{0.716} and \devnum{0.486}, with
\devnum{189.3}, \devnum{199.7} and \devnum{219.6} s. Near saturation, a window large enough
to help lets long jobs wait longer than the guard does. The last test corrupts the scores
deliberately, by reversing them, by drawing them at random, or by calling the longest
\devnum{1\%} of jobs the shortest. The unguarded policy then delays some job by more than
\devnum{6{,}000} s beyond FCFS at the busiest load, and Guard($\devnum{600}$) keeps every job
within its bound. The guarded $\mathrm{p99}_{\mathrm{dl}}$ is then \devnum{546.5},
\devnum{261.9} and \devnum{543.6} s, against FCFS's \devnum{273.50} s. Over these three
corrupted scores and the five protocols of Section~\ref{sec:exp_visibility}, Guard($\devnum{600}$)
closes between \devnum{$-1.187$} and \devnum{0.801} of the gap at the busiest load, while
its largest excess over FCFS stays between \devnum{517.7} and \devnum{534.8} s. The guarantee thus
protects the worst single job but not the shape of the tail, and a guarded run may have a
worse percentile than FCFS, by at most $G$ (Supplementary Table~S8).
% Sources: prechecks/referee_round4/maxwait_overlay0.csv (Block(512) rows, levels 0-2) and
%   prechecks/timeout_rule/timeout_overlays.csv (overlay 0, Guard(300));
%   outputs/dev_tables/main_table.csv rows 83-88 (corrupted scores at rho 1.0).
%   Range over eight rows at level 2: Guard(600)|{original,exact,online,conservative,static}
%   (exact_comparison.csv, online_comparison.csv) and Guard(600)-{reversed,random,top1short}
%   (main_table.csv): gap_closed -1.187..0.801, max_excess_s 517.7..534.8.

\subsection{The Bounds on the Traces}
\label{sec:exp_bound}
\label{sec:exp_k1}

Every run that includes a bounded policy checks the bound on every job inside the
simulation, so a violation would expose an error in the code rather than in the theorem.
The \devnum{315} policy-by-cell runs reported here make \devnum{5{,}554{,}949{,}400} such
checks. The single-server runs add \devnum{67{,}396{,}287}, and an independently written
second simulator adds \devnum{5.7} billion. None of them finds a violation, and the count of
violations is \devnum{0}. Proposition~\ref{prop:clock} was checked job by job on the same
overlays and Proposition~\ref{prop:absolute} on all \devnum{304} bounded runs, also with
\devnum{0} violations. Proposition~\ref{prop:late} was checked on constructed instances
(Supplementary Section~S4.12). The ratio of the realised worst excess to the bound falls as
$k$ grows, from \devnum{0.9950} at $k = 1$ to \devnum{0.8850} at $k = 8$. At one server
Guard($\devnum{600}$) reaches \devnum{595.5} s of its \devnum{600} s bound (Supplementary
Section~S3.6). The residual $\Delta_i$ of Theorem~\ref{thm:identity} never exceeds
\devnum{0.518} of its own bound $2(k-1)L$ (Supplementary Sections~S3.13 and~S3.16).
% Source: outputs/dev_tables/bound_checks.csv (315 rows, jobs_checked summing to
%   5,554,949,400, violations 0, used_over_allowed maxima 0.9311 at k = 4 ... 0.8850 at
%   k = 8); outputs/dev_tables/k1/bound_checks.csv (21 rows, 67,396,287 checks, worst
%   0.9950); outputs/dev_tables/identity_residuals.csv (max ratio 0.5172712).

The absolute bound of Proposition~\ref{prop:absolute} comes close to the realised waits. At
one server every FCFS wait equals its fluid backlog to the microsecond, as the proposition
predicts, and the largest wait reaches \devnum{0.999} of the bound. At the three loads the
worst wait under a work budget reaches \devnum{0.886}, \devnum{0.951} and \devnum{0.963} of
the bound, and under FCFS \devnum{0.881}, \devnum{0.945} and \devnum{0.965}. An absolute
bound quoted in advance needs a forecast of the burst $\sigma$. The burst of the validation
terms predicts that of the evaluation terms only to within a factor of \devnum{0.738} to
\devnum{1.573}, and it is too low in \devnum{10} of \devnum{15} cells. A bound quoted from
it is broken in \devnum{18} of \devnum{60} runs, by up to \devnum{349.8} s. Such a bound
therefore needs a margin, here a factor of \devnum{1.36} on $\sigma$, or an admission
control that keeps the arrivals within the quoted burst.
% Sources: prechecks/envelope_bound/out_summarise_tight.txt and out_envelope_bound.txt:21;
%   out_tight_bound.txt:310-336.

\subsection{Where the Method Applies}
\label{sec:exp_cross}

Table~\ref{tab:cross} places the main log beside the other workloads. Excesses are in units
of each log's $L$, the unit in which the guarantee is written. For the grading logs $L$ is
the time limit, and for the others it is the analysis cap of Section~\ref{sec:fields}. Two screens say
where the guard is useful. The first compares the FCFS tail with the guarantee's additive
term $(3-2/k)L$, which is close to $3L$. If the FCFS p99 is below this term, the guarantee
is coarser than the waits themselves. CodeBench, at \devnum{4.56}$L$, and the cloud-function
log, at \devnum{4.22}$L$, clear the first screen, and four of the six workloads do not. The
compute farm is the extreme case, with an FCFS p99 of \devnum{18.7} h against $L =
\devnum{493{,}857}$ s, or \devnum{5.7} days. The second screen measures how concentrated the
costs are, which is the opportunity for reordering. In the two CI pools the longest
\devnum{1\%} of jobs carry only \devnum{3.4\%} and \devnum{5.2\%} of the work. There,
ordering by true cost makes the p99 worse in three of the four windows we examined. Where
both screens are cleared, the guard closes \devnum{0.48} to \devnum{0.71} of the gap and
holds the largest excess to \devnum{2.5} to \devnum{3.8}$L$ at a bound of \devnum{5}$L$.
Clearing both screens does not guarantee a gain, as the next subsection shows.

% Wider than the MDPI text block: set across the full page with the template's
% adjustwidth/\extralength mechanism.
\begin{table}[htbp]
\begin{adjustwidth}{-\extralength}{0cm}
\centering
\caption{The workloads under one format. Tail share: fraction of executed work carried by
the longest \devnum{1\%} of jobs. AUROC: heavy label from information available at arrival.
Screen 1: whether the FCFS p99 exceeds $(3-2/k)L$. Excess in units of each log's $L$. A
negative gap closed means that the policy's p99 is worse than FCFS's. CodeBench row: original
protocol. Supplementary Section~S6.7 describes the CI capacities and adds a log from a grid
computing cluster.}
\label{tab:cross}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lccccccc}
\toprule
 & Tail & & FCFS & Screen & Gap closed, & Unguarded & Guard at $G \approx \devnum{5}L$ \\
Log and load & share & AUROC & p99$/L$ & 1 & SPJF-E & max excess & gap closed / max excess \\
\midrule
CodeBench, $\rho \approx \devnum{1.0}$ & \devnum{0.375} & \devnum{0.936} & \devnum{4.56} & yes & \devnum{0.916} & \devnum{98.4}$L$ & \devnum{0.478} / \devnum{3.82}$L$ \\
ACcoding, drawn arrivals, $\rho = \devnum{0.8}$\textsuperscript{a} & \devnum{0.122} & \devnum{0.927} & \devnum{0.021} & no & \devnum{0.980} & \devnum{20}$L$ & \devnum{0.974} / \devnum{2.21}$L$ \\
Azure Functions, $\rho = \devnum{1.0}$ & \devnum{0.443} & \devnum{0.985} & \devnum{4.22} & yes & \devnum{0.858} & \devnum{10.07}$L$ & \devnum{0.710} / \devnum{2.52}$L$ \\
CI pool A, $k = \devnum{156}$ of \devnum{173} & \devnum{0.034} & \devnum{0.651} & \devnum{1.78} & no & n/a\textsuperscript{b} & \devnum{11.53}$L$ & \devnum{0.735} (mean)\textsuperscript{b} / \devnum{2.64}$L$ \\
CI pool B, $k = \devnum{96}$ of \devnum{105} & \devnum{0.052} & \devnum{0.997} & \devnum{0.373} & no & \devnum{0.958} & \devnum{0.34}$L$ & \devnum{0.958} / \devnum{0.34}$L$ \\
Intel Netbatch, $\rho = \devnum{0.95}$ & \devnum{0.372} & \devnum{0.890} & \devnum{0.136} & no & \devnum{$-4.111$} & \devnum{0.85}$L$ & \devnum{$-4.346$} / \devnum{0.83}$L$ \\
\bottomrule
\end{tabular}

\vspace{2pt}
\begin{minipage}{\fulllength}
\footnotesize
\textsuperscript{a} ACcoding arrivals are drawn, so this row describes the ordering and not
waiting on that platform. Its budget is the largest tested, $B = \devnum{10}L$, and its tail
share is simulated with $L = \devnum{30.5}$ s (\devnum{0.275} on measured cost at $L =
\devnum{30.0}$ s, the figure of Section~\ref{sec:intro}).
\textsuperscript{b} On CI pool A, SJF is itself worse than FCFS at the p99, so no p99 gap is
defined; the guard's entry is the gap in mean wait and is not comparable with the other
rows.
\end{minipage}
\end{adjustwidth}
\end{table}
% Source of Table~\ref{tab:cross}: as before this revision (CodeBench row from
%   Table~\ref{tab:guard} and outputs/dev_predictor/predictor_metrics.csv; ACcoding, Azure and
%   Netbatch rows from evidence/cross_domain/out_cross_domain_table.txt sections A-D, the
%   ACcoding guard entry from evidence/accoding_v2/out_accoding_v2.txt; CI rows from
%   evidence/firefox_ci/out_SUMMARY.txt sections A, C, D, E, at the whole-window fitted
%   capacities k_eff = 156 and 96). The two LPC-EGEE rows moved to Supplementary
%   Section S6.7 with their source comments.

\subsection{Capacity, Physical Runs and Feedback}
\label{sec:exp_physical}
\label{sec:exp_sens}

The benefit depends on capacity. We ran every $k$ from \devnum{1} to \devnum{20} on the five
overlays. At $k = \devnum{1}$, Guard($\devnum{600}$) makes $\mathrm{p99}_{\mathrm{dl}}$ worse
by \devnum{493.79} s, and all five overlays agree that it improves the p99 only for $k =
\devnum{4}$ to \devnum{12}. We also ran the guard physically, on two worker processes that
occupy real time slots for each job's recorded duration. This run executed
\devnum{2{,}151} jobs of one development window, and all \devnum{6{,}453} dispatch decisions
followed the rule. Against FCFS on the same run's measured arrivals and running times, every
job stayed within the promise of Guard($\devnum{300}$), with a largest excess of
\devnum{222.21} s. A physical run needs the bound widened by the time workers sat idle
during dispatch, at most \devnum{1.127} s here, and no job came close to needing it. Both processes ran on one machine, so this run tests the dispatch
rule, not network delay. The guarded p99 of \devnum{1{,}783.65} s was nevertheless no better than
FCFS's \devnum{1{,}763.30} s, and single jobs differed from the ideal simulation by up to
\devnum{1{,}354.10} s. A second physical test used the two-worker back end of a course
platform, where twenty scripted accounts submitted \devnum{100} jobs per cell in a
deadline-shaped burst with $G = \devnum{400}$ s. Unguarded predicted order left \devnum{7}
jobs beyond the bound. The four guarded cells left none, with a worst excess of
\devnum{0.514}$G$ (Supplementary Sections~S6.1, S6.2 and~S6.9).
% Sources: prechecks/weakness1_attack/summary_numbers.json and capacity_curve.csv;
% prechecks/weakness1_attack/physical_numbers.json and physical_verification.json;
% prechecks/weakness1_attack/stall_bound_check.json (G_ns 300e9, jobs 2151,
% maximum_uncorrected_excess_ns 222207551100, maximum_additive_correction_twice_ns / 2
% = 1.1265 s, corrected_violations 0);
% prechecks/platform_testbed/records/summary.json (jobsOverPromise, ratio 0.5138).

The simulations replay arrivals at their recorded times, so they neither reward a scheduler
with the work it would attract nor punish it for the users it would drive away
\cite{feitelson2021resampling,shmueli2009simulation}. The logs record one part of that
feedback, the time between one result and the same student's next submission. In a second
replay, each submission is released only after its predecessor's result plus that recorded
delay. This replay shortens $\mathrm{p99}_{\mathrm{dl}}$ by a factor of \devnum{1.3} to
\devnum{3.8}. Guard($\devnum{600}$) then closes \devnum{0.825}, \devnum{0.809} and
\devnum{0.810} of the gap at the three loads, against \devnum{0.734}, \devnum{0.827} and
\devnum{0.800} with the recorded arrival times, and it keeps every job within its bound
(Supplementary Section~S6.5).
% Sources: prechecks/closed_loop/out/open_vs_closed_mean.csv, p99_dl_s and gap_closed for
% FCFS and Guard(600) at mode=open and mode=closed/late_submissions=kept, levels 0-2.
